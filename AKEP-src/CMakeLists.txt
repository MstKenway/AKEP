project(AKEP_SRC)
aux_source_directory(src/. DIR_AKEP)
add_library(${PROJECT_NAME} ${DIR_AKEP})
target_include_directories(${PROJECT_NAME} PUBLIC ${PROJECT_SOURCE_DIR}/include)
target_link_libraries(${PROJECT_NAME} PUBLIC WjCryptLib)
target_link_directories(${PROJECT_NAME} PUBLIC WjCryptLib)
